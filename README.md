# 五代麥樂獸拼圖配點

一個基於 React 和 TypeScript 的角色碎片分配計算器,幫助您優化遊戲角色的屬性配置。

## 功能特點

- 🎮 多角色支援與管理
- 📊 即時屬性計算與統計分析
- 🎯 智能碎片分配優化
- 📈 視覺化數據圖表展示
- 💾 本地數據持久化儲存

## 技術棧

- **React 19** - 前端框架
- **TypeScript** - 類型安全
- **Vite** - 快速構建工具
- **Recharts** - 數據視覺化
- **Lucide React** - 圖標庫

## 開發環境要求

- Node.js 18.0 或更高版本
- npm 或 yarn 或 pnpm

## 快速開始

### 安裝依賴

```bash
npm install
```

### 開發模式

```bash
npm run dev
```

應用將在 `http://localhost:3000` 啟動。

### 構建生產版本

```bash
npm run build
```

構建產物將生成在 `dist` 目錄。

### 預覽生產構建

```bash
npm run preview
```

## 部署

### GitHub Pages

專案已配置自動部署到 GitHub Pages。當你推送到 `main` 分支時，會自動觸發部署流程。

**首次設置：**

1. 前往 GitHub 儲存庫設定
2. 進入 **Settings** → **Pages**
3. 在 **Source** 下選擇 **GitHub Actions**
4. 推送代碼後，GitHub Actions 會自動構建並部署

部署後可訪問：`https://tw092669-ctrl.github.io/26cm/`

## 專案結構

```
.
├── components/          # React 組件
│   ├── CharacterSelector.tsx
│   ├── ShardSummary.tsx
│   └── StatControl.tsx
├── App.tsx             # 主應用組件
├── index.tsx           # 應用入口
├── types.ts            # TypeScript 類型定義
├── constants.ts        # 常數配置
├── metadata.json       # 應用元數據
├── vite.config.ts      # Vite 配置
└── tsconfig.json       # TypeScript 配置
```

## 環境變數

如需使用 Gemini API，請在專案根目錄創建 `.env.local` 檔案：

```env
GEMINI_API_KEY=your_api_key_here
```

## 瀏覽器支援

- Chrome (最新版)
- Firefox (最新版)
- Safari (最新版)
- Edge (最新版)

## License

MIT

## 相關連結

- [AI Studio App](https://ai.studio/apps/drive/1OlHarFO0cPCX8pEenR4CI7cU-3dhlVls)
