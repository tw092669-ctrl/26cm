# InventoryMaster Pro - 專業庫存管理系統

這是一個基於 React + Vite + TypeScript 開發的現代化庫存管理應用程式。專為產品建檔、分類管理與展示所設計，並整合了 Google Gemini AI 進行智慧分類與 Google Sheets 進行資料同步。

## ✨ 主要功能

*   **精美介面**：使用 Tailwind CSS 打造的響應式卡片設計與列表檢視。
*   **智慧建檔**：整合 Google Gemini AI，輸入產品名稱即可自動建議分類屬性（品牌、能力、種類等）。
*   **自訂分類**：支援動態新增與刪除分類選項（品牌、管徑、尺寸等）。
*   **客製化外觀**：可針對個別分類項目設定專屬字體顏色。
*   **Google 試算表同步**：
    *   **匯入**：直接從 Google Sheets 讀取產品資料。
    *   **匯出**：一鍵複製格式化資料，可直接貼回 Google Sheets。
*   **資料匯出**：支援匯出為 CSV 表格或將產品列表轉存為 PNG 圖片。
*   **純前端架構**：資料儲存於瀏覽器 LocalStorage，隱私安全且部署容易。

## 🚀 快速開始

### 前置需求
*   Node.js (建議 v16 以上)
*   npm 或 yarn

### 安裝依賴

```bash
npm install
```

### 本地開發

```bash
npm run dev
```

啟動後，開啟瀏覽器訪問 `http://localhost:5173`。

### 建置生產版本

```bash
npm run build
```

建置後的檔案將位於 `dist` 資料夾中。

## 🔑 環境變數設定 (Google Gemini AI)

若要使用 AI 自動填寫功能，您需要一組 Google Gemini API Key。

1.  前往 [Google AI Studio](https://aistudio.google.com/) 申請 API Key。
2.  在專案根目錄建立 `.env` 檔案（或在部署平台設定環境變數）。
3.  加入以下內容：

```env
API_KEY=您的_GEMINI_API_KEY
```

> 注意：本專案為純前端應用，API Key 在建置時會被打包進程式碼中。若為公開專案，建議設定 API Key 的網域限制以確保安全。

## ☁️ 部署至 GitHub Pages

本專案已設定好 Vite 配置以支援 GitHub Pages 部署。

1.  **修改設定檔**：
    打開 `vite.config.ts`，將 `base` 屬性修改為您的儲存庫名稱：
    ```typescript
    export default defineConfig({
      // ...其他設定
      base: '/您的儲存庫名稱/', // 例如: '/inventory-app/'
    });
    ```

2.  **推送到 GitHub**：
    建立一個新的 GitHub Repository 並推送程式碼。

3.  **設定 GitHub Actions (推薦)**：
    在 GitHub Repository 的 `Settings` > `Pages` 中，選擇 `GitHub Actions` 作為來源，並使用預設的 Static HTML workflow，或者手動部署 `dist` 資料夾。

4.  **手動部署 (替代方案)**：
    您也可以使用 `gh-pages` 套件進行部署：
    ```bash
    npm install gh-pages --save-dev
    ```
    在 `package.json` 的 `scripts` 中加入：
    ```json
    "deploy": "gh-pages -d dist"
    ```
    然後執行：
    ```bash
    npm run build
    npm run deploy
    ```

## 📊 Google Sheets 整合說明

若要使用匯入功能：
1.  您的 Google 試算表需設定為「知道連結的人皆可檢視」或是使用 Google Cloud Platform 申請一組具備 Sheets API 權限的 API Key。
2.  在 APP 的設定頁面中輸入：
    *   **API Key**: 您的 Google Sheets API Key。
    *   **Spreadsheet ID**: 試算表網址 `/d/` 與 `/edit` 中間的字串。
    *   **工作表名稱**: 例如 `Sheet1`。

## 🛠️ 技術棧

*   [React 18](https://reactjs.org/)
*   [TypeScript](https://www.typescriptlang.org/)
*   [Vite](https://vitejs.dev/)
*   [Tailwind CSS](https://tailwindcss.com/)
*   [Google Gemini SDK](https://ai.google.dev/)
*   [Lucide React](https://lucide.dev/) (Icons)
